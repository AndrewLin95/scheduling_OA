const single = "single";
const double = "double";
const commercial = "commercial";

const certified = "certified";
const pendingCert = "installer pending certification";
const laborer = "laborer";

const buildings = [single, double, commercial]

const schedule = (buildings, employees) => {
  return null;
}